#include <Servo.h>

Servo myServo;
int angle = 90;

void setup() {
  Serial.begin(9600);
  myServo.attach(9);
}

void loop() {
  if (Serial.available() > 0) {
    angle = Serial.parseInt();   // legge angolo da Python
    angle = constrain(angle, 0, 180);
    myServo.write(angle);
  }
}
